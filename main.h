#ifndef Main_H
#define Main_H

#include <stdlib.h>				/* library to use malloc - Free Malloc. */
#include <stdarg.h>				/* library to use variadic fonctions. */


int _printf(const char *format, ...);
/* Function that display output according to given format. */

int _putchar(char c);
/* To write a single character to standard output (stdout). */

#endif
